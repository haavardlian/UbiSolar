<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sintef_energy.ubisolar" >
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.DrawerActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.sintef_energy.ubisolar.LoginActivity"
            android:label="@string/title_activity_login"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>
    <provider
            android:name="com.sintef_energy.ubisolar.database.energy.EnergyProvider"
            android:authorities="com.sintef_energy.ubisolar.db.EnergyContract"
            android:exported="true"
            android:grantUriPermissions="true"
            android:label="AlarmsProvider"
            android:readPermission=".alarm.db.AlarmModel.READ"
            android:writePermission=".alarm.db.AlarmModel.WRITE" />
    </application>

</manifest>
